machine:
  node:
    version: 6.1.0
dependencies:
  override:
    - yarn install
test:
  override:
    - npm run cov
  post:
    - ./node_modules/.bin/codeclimate-test-reporter < ./coverage/lcov.info
